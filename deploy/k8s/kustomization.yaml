apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: deepforge

resources:
  - namespace.yaml
  - secrets.yaml          # not tracked in git — copy secrets.yaml.example to create it
  - pvc.yaml
  - controller.yaml
  - ingress.yaml

# Override these in overlays for different environments:
# images:
#   - name: deepforge
#     newName: myregistry.azurecr.io/deepforge
#     newTag: v1.0.0
